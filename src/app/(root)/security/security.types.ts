export type EntryRow = {
  id: number;
  period: string;
  vehicleType: string;
  vehicleNumber: string;
  status: string;
  reason: string;
  registrar: string;
  responsible: string;
};

export type EntryRow1 = {
  id: number;
  period: string;
  vehicleNumber: string;
  status: string;
};